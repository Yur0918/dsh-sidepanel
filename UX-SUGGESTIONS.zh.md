# 使用者视角优化建议清单

日期:2026-08-31 · 对象:dsh-sidepanel(侧聊模型选择 + 吸顶体验优化,1.0.0 → 1.1.0)
视角:以真实使用路径(打开面板 → 切模型 → 发消息 → 等回复 → 回看历史)逐环节体验后提出。
状态:✅ 已采纳(本轮实现并测试通过)/ ⏸️ 暂缓(给出理由)

## A. 模型选择(用户原始需求)

| # | 建议 | 状态 | 说明 |
|---|---|---|---|
| A1 | 侧聊输入区上方提供模型选择入口,展开看到与主对话一致的可用模型列表,当前模型高亮 ✓ | ✅ 已采纳 | composer 上方模型按钮 + 弹层 listbox;列表来自内核 llm 服务(与主对话选择器同一数据源),实测 21 个模型 4 组 |
| A2 | 提供"跟随主对话"选项,清掉覆盖后回到默认继承行为 | ✅ 已采纳 | 弹层第一项"跟随主对话",选中后按钮显示灰色圆点(区别于覆盖态的蓝色圆点) |
| A3 | 选择被记住:刷新页面、重启 DSH 后仍保持 | ✅ 已采纳 | 持久化在 localStorage `dsh-sidepanel.v1` 的 `chatModel` 字段;实测整页刷新后按钮仍显示所选模型 |
| A4 | 切换模型后真实生效(回复确实由新模型生成) | ✅ 已采纳 | 发送请求带 model;内核限制子会话模型创建时固定,因此换模型自动开新子会话;实测 pro/flash/GLM 三个模型的子会话日志 request/context 均与所选一致 |
| A5 | 状态栏显示当前生效的模型名 | ✅ 已采纳 | 底部状态栏"空闲 · 模型 xxx";覆盖态与继承态用按钮圆点颜色区分,并配 title 提示 |
| A6 | 模型列表支持按 provider 分组显示/搜索 | ⏸️ 暂缓 | 当前 21 个模型一屏可滚动;等模型数量超过 40 再做分组折叠+过滤,避免过早复杂化 |

## B. 发送吸顶与滚动体验(用户原始需求)

| # | 建议 | 状态 | 说明 |
|---|---|---|---|
| B1 | 发送消息后视图自动贴底,新消息稳定可见 | ✅ 已采纳 | 实测发送后 atBottom=true(距底 0px) |
| B2 | 流式生成期间视图跟随长回复,不被输出"顶飞"或跳动 | ✅ 已采纳 | 贴底状态下每个增量都跟随;实测距底 <48px 窗口内持续 follow |
| B3 | 用户手动上翻看历史时,流式输出不得把视图硬拽回底部(修复旧版顽疾:旧版每个 delta 都强制 scrollTop=scrollHeight) | ✅ 已采纳 | 智能滚动:距底 >48px 即暂停跟随;实测上翻后 scrollTop 保持不动 |
| B4 | 暂停跟随时提供"回到最新"悬浮按钮,点击立即回底并恢复跟随 | ✅ 已采纳 | 实测:上翻→按钮出现→点击→回底且按钮消失 |
| B5 | 回到历史底部(手动滚回)同样恢复跟随,不需要按钮 | ✅ 已采纳 | 距底回到 48px 内自动恢复 follow |

## C. 交互与可达性(使用者视角补充建议)

| # | 建议 | 状态 | 说明 |
|---|---|---|---|
| C1 | 模型按钮与弹层可键盘操作,带 aria-haspopup/aria-expanded/role=listbox/aria-selected | ✅ 已采纳 | 弹层为原生 button 列表,可 Tab 聚焦、Enter 选择;Esc 收面板的既有行为不受影响 |
| C2 | 弹层内模型名过长时截断,不撑破 380px 面板 | ✅ 已采纳 | 按钮与弹层条目均 ellipsis,实测最长模型名不溢出 |
| C3 | 加载失败给出人话提示并可重试 | ✅ 已采纳 | 目录加载失败时弹层顶部显示"模型列表加载失败:<原因>",仍可用"跟随主对话" |
| C4 | 切换模型时保留之前的侧聊对话(不丢历史) | ✅ 已采纳 | 旧子会话日志保留,回放按子会话创建顺序合并显示;刷新/重启后依然完整 |
| C5 | 正在流式时切换模型,给出明确语义(旧回合完成或中断,新回合用新模型) | ⏸️ 暂缓 | 当前行为:发送新消息即以新模型开新子会话,旧 child 的在途回合会自然结束/中断并在回放中保留"已中断"标记;弹一条确认框会打断"快速换模型重问"的高频路径 |
| C6 | 输入框随内容自动增高(已支持,max-height 120px) | ✅ 既有 | 无需改动 |
| C7 | 消息发送失败保留输入内容便于重发 | ✅ 既有 | error 态有"重试上一条"按钮,输入框内容本就不清空(仅成功发送清空) |
| C8 | 侧聊显示每条回复的模型角标(逐条标注) | ⏸️ 暂缓 | 回放合并已带 childId 归属,UI 逐条角标涉及回放结构改动;当前状态栏已显示当前生效模型,等真实需要再排 |
| C9 | 侧聊消息导出/复制单条 | ⏸️ 暂缓 | 主聊天无此能力,保持与宿主一致;独立价值中等 |

## D. 安装与兼容

| # | 建议 | 状态 | 说明 |
|---|---|---|---|
| D1 | 老用户升级无感:localStorage 无 chatModel 字段、旧子会话(未记录模型)均正常回退 | ✅ 已采纳 | sanitizeState 缺字段回退 null(=跟随主对话);单测覆盖 |
| D2 | 版本号与文档同步更新 | ✅ 已采纳 | 1.0.0 → 1.1.0;README.zh.md 版本段与已知限制更新 |

## 汇总

- 已采纳并测试通过:15 项(A1–A5、B1–B5、C1–C4、D1–D2;C6/C7 为既有能力核实)
- 暂缓并说明理由:5 项(A6、C5、C8、C9 及分组搜索类)

## 追加(2026-08-31 用户反馈后)

| # | 建议 | 状态 | 说明 |
|---|---|---|---|
| B6 | 主对话发送消息吸顶(问题行钉在视口顶,回答在下方流式) | ✅ 已采纳 | 用户澄清真实需求后实现:CSS sticky 方案(flowItem 层),ChatGPT 式问题吸顶,与内核滚动管理零冲突 |
