# CHANGELOG 1.3.5 — 修复:折叠条滚回原位不展开 + 合并为单按钮(移除悬停折叠)

日期:2026-09-03 · 范围:`lib/client.js` · 零内核改动

## 问题(用户实测反馈)

1. **折叠的长消息回滚到消息处,没有正常展开**;
2. **折叠/展开按钮位置怪异**(悬停「折叠 ⌃」飘在行左缘,气泡在右侧,两者割裂),建议合成一个按钮。

## 根因 1:展开滞回阈值对首行消息是负数,永不满足

1.3.4 的展开条件是 `st >= top - 60`(滚回自然位置上方 60px 才展开)。但会话第一条消息
的自然位置 top≈16,阈值 16-60 = **-44 永远达不到**——首行一旦折叠就永不自动展开。

这个 60px 滞回带本意是防止边界抖动,前提是"展开会引起布局跳动";而 1.3.4 的
下一兄弟补偿方案保证折叠/展开**零位移**(布局总高不变),滞回带已无存在意义。

修复:**折叠状态直接跟随钉住决策**(collapsed ⟺ pinned && tall && 未手动展开)。
钉住/取消钉住的阈值本来就是稳定的(top 缓存),同一阈值切换折叠不存在自激——
回滚到消息自然位置时必然展开。

## 根因 2:两个按钮两套位置

悬停「折叠 ⌃」(行左缘 `left:8`)与钉住「展开 ⌄ / 收起 ⌃」(行右上 `top:3 right:6`)
并存:一个在左一个在右、一个悬停一个常驻,用户截图实测"折叠按钮孤零零飘在画面左缘,
气泡在右侧"。

修复:**移除整个手动悬停折叠系统**(FOLD_CLS/FOLDABLE/fold-bar/foldRow/unfoldRow/
applyFoldPass/foldSummary,约 200 行),只保留钉住行右上角**一个**切换按钮:
折叠条上显示「展开 ⌄」,展开态显示「收起 ⌃」。钉住=折叠,原位=完整,不再需要第二套
折叠形态。角标位置微调(top:6 right:10)。

## 实测验证(真实页面 DOM 断言)

- 底部打开:折叠 1 + 钉住 76px + 「展开 ⌄」,悬停折叠类 0 个;
- st=200:仍折叠钉住(pinTop 恒 84);**st=0:完全展开**(collapsed 0,旧版永不展开);
- 回到底部:重新折叠钉住;「展开 ⌄ ⇄ 收起 ⌃」往返正常(76px ⇄ 942px,补偿正确挂载/清除);
- 全程仅一个角标;测试 28 项全过(移除 foldSummary 用例)。

## 设计边界

展开态(942px)钉住时,接近会话底部仍会被容器底边推出——这是用户主动展开阅读的
固有几何,收起后立即恢复全程贴顶。
