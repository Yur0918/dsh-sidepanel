# CHANGELOG 1.3.8 — 1.3.7 收口:补偿状态机回归测试 + 徽标语义修正

日期:2026-09-05 · 范围:`lib/client.js` + `test/` · 零依赖新增

## 变更

- **rest-comp 状态机提为模块级 + 回归测试**:`sweepStaleComp` / `applyRestComp` /
  `clearRestComp` 原本埋在 apply() 闭包里,1.3.7 的 blocker(补偿永不回收)正是漏测
  的结果。三者无闭包依赖,原样上移到模块级并加入 `_pure` 导出;新增 2 组假 DOM 桩
  测试锁定行为:apply 保存原值且重 apply 不覆盖、clear 完整还原(无原值则移除)、
  sweep 恢复孤儿标记并跳过 keep 节点。
- **产物徽标语义**:徽标数字此前恒等于 `counts.total`(含扫描来源),而「含扫描来源」
  关闭时「全部」视图并不显示扫描行——徽标承诺了视图没有的文件。现改为跟随开关:
  关=工具产出数,开=全部数,徽标与视图所见一致。

## 验证

- 测试 32/32 全绿(新增 2 组),`npm run check` 通过。
