# CHANGELOG 1.3.0 — 主对话吸顶折叠 + 手动折叠 + 产物视图疏导

日期:2026-09-03 · 范围:仅 `lib/client.js`(CSS + 吸顶应用逻辑 + 1 个纯函数)+ 测试 · 零内核改动,零依赖新增

## 问题

用户反馈:发送的消息一长(如整段定时任务提示词),吸顶的大气泡把视口上半部整个盖住,
下面的思考内容和回复内容都从气泡底下穿过,基本不可读。

根因:吸顶方案是给最新用户行加 `position:sticky;top:8px` + 不透明底,消息气泡本身多高,
钉住后盖住的带子就多高——短消息是 ChatGPT 式便利,长消息变成一堵墙。

## 方案:钉住时把长气泡折叠成紧凑条

**不能用 `max-height`**:sticky 元素仍在文档流里,压掉气泡高度会让下方所有内容瞬间上移
几百像素(滚动跳位),且自然位置缓存全部失效,吸顶判定会抖动。

采用 **`clip-path: inset(0 0 calc(100% - 76px) 0 round 10px)`**:

- 只裁视觉、不动布局——气泡在文档流里的占位保持自然高度,钉住/展开/收起零跳动;
- 被裁掉的不可见区域 `pointer-events:none`,回复内容从透明区穿过且可正常选中/点击;
- 可见条底部 30px 渐隐(`::after` 渐变到气泡底色,暗色模式走 `--dsw-alias-bg-layer-1`);
- 右上角「展开 ⌄ / 收起 ⌃」角标(`.dsp-sticky-toggle`,absolute 定位不影响布局)。

### 判定纯函数 `stickyCollapseFor({ pinnedHeight, userExpanded })`

- `pinnedHeight ≤ 100px`(`STICKY_COLLAPSE_THRESHOLD_PX`)→ `none`(短消息原样钉住,无角标);
- 超过阈值且用户未展开 → `collapsed`;
- 用户点过「展开」→ `expanded`;钉住行切换/新消息发送时展开状态自动复位。

### 其他

- `measureStickyRows` 剥类时同步剥折叠类(clip-path 不影响布局,属双保险);
- 角标被 React 重渲染冲掉后由下一次 `mainChatApplySticky`(rAF 合帧)自动补挂;
- 修复 `package.json` 的 `npm test` 在 Node 22.22 下无法运行的脚本写法(目录形式不被
  `--test` 接受,改为显式列出三个测试文件);
- 版本号从 1.1.0 补齐到 1.2.1(1.2.0 的变更此前只写了 CHANGELOG 文件没升版本字段)。

## 测试

- 纯函数测试 +1(`stickyCollapseFor` 阈值/折叠/展开三分支),26 → 27 项,`node --test` 全绿;
- `npm run check` 通过。

## 1.3.0 追加(同日第二批):手动折叠 + 产物视图疏导

### 主对话「手动折叠」长消息

- 超过阈值(100px)的历史用户消息,悬停时右上角出现「折叠 ⌃」角标,一键折成一行摘要条
  (首行 + 共 N 字 · 展开 ⌄),点摘要条任意处展开——翻历史时不用再被旧长提示词淹没。
- 与吸顶折叠互补且互斥:当前被吸顶钉住的行不显示折叠角标;手动折叠过的行被钉住时,
  直接以摘要条作为钉住形态(自动收起吸顶折叠角标,避免两套控件打架)。
- 折叠直接回收高度(在流消息没有 sticky 那个 max-height 跳位问题),折叠/展开发生在
  视口上方时用 scrollTop 前后差值补偿,视图不跳(浏览器 scroll anchoring 之外的决定性兜底)。
- 实现为写幂等的 `applyFoldPass`(每轮内容变更跑一次;空转时零 DOM 写入,不会触发
  MutationObserver 自反馈循环);折叠状态不持久化,会话重渲染后还原为展开。
- 纯函数 `foldSummary(text, maxLen)`(首非空行截断 + 字数统计)入 `_pure` 导出。

### 产物视图:扫描数据常开 + 空状态疏导

- `/sidepanel/artifacts` 改为始终带 `scan=1`(扫描上限 4000 目录/300 文件,开销有界):
  「工作区扫描」chip 不再依赖「含扫描来源」开关先打开才有数据。
- 「含扫描来源」开关语义收窄:只决定「全部」视图里是否混入扫描来源(默认关,保持原观感)。
- 空状态升级:会话确实没有 write/edit 产物时,标题明确显示「本会话没有文件工具产物」,
  并提示扫描发现的文件数,提供「查看工作区扫描 (N)」一键跳转——解决「当前对话的产物
  怎么是 0」的困惑(如定时播报会话:报表由插件写入,不经过会话工具,只出现在工作区扫描里)。

### 测试

- 纯函数测试 +1(foldSummary:首行提取/空文本/长文本截断/字数),27 → 28 项全绿。

## 1.3.1 修复:折叠角标在长对话里消失 + 位置改左下角

- **根因**:消息列表是虚拟化渲染,滚动时行节点被回收重建,append 的折叠角标随节点一起
  消失;而折叠对账(applyFoldPass)此前只挂在内容变更路径上,纯滚动不触发 → 历史区几乎
  永远看不到角标。修复:对账挪进 mainChatApplySticky 的类管理循环之后、pick<0 早退之前
  ——每次 scroll 都自愈(与吸顶角标同一自愈通道),schedule 里的独立调用随之移除。
- **位置**:角标从行右上角(top:3px/right:6px,压着右对齐气泡的文字)移到**左下角**
  (left:8px/bottom:4px)——用户气泡靠右,左下是空区,悬停出现不挡内容、也更可发现。

## 1.3.2 修复:单条长消息的会话里折叠角标永远不出现

- **根因**:会话打开时自动滚到底,唯一的用户消息立刻越过顶线被吸顶钉住;而互斥规则
  「钉住行不显示折叠角标」使这条消息——会话里唯一可折的行——永远拿不到角标
  (定时播报会话正是这种单消息结构,用户实测复现)。
- **修复**:钉住行保持可折叠。折叠后钉住形态从 76px 渐隐条换成一行摘要条(高度更省);
  未折叠时钉住行的角标从行底部(clip 折叠态下在视口外 500px 处)移到左上角可见区。
- 展开钉住的折叠行会回到吸顶默认形态(超过阈值则自动 clip 折叠),行为闭环。
